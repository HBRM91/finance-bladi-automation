name: Finance Bladi
on: 
  schedule: [{cron: '0 7 * * *'}]
  workflow_dispatch:

jobs:
  automate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v4
        with: {python-version: '3.10'}
      - name: Install dependencies
        run: pip install gspread google-auth
      - name: Setup credentials
        env: {GOOGLE_CREDENTIALS: ${{secrets.GOOGLE_CREDENTIALS}}}
        run: |
          echo "$GOOGLE_CREDENTIALS" > credentials.json
          # Validate it's proper JSON
          python -c "import json; json.load(open('credentials.json')); print('✅ credentials.json is valid JSON')"
      - name: Run simplified automation
        run: |
          # Create simple script
          cat > github_main.py << 'EOF'
          # [PASTE THE SIMPLIFIED VERSION ABOVE HERE]
          EOF
          python github_main.py